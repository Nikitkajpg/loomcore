package com.njpg.loomcore.ui.screen

/**
 * Перечисление экранов верхнего уровня приложения.
 *
 * Навигация между ними происходит в корневом [App]:
 * сначала показывается [Splash] (анимация загрузки),
 * затем после завершения — [Main] (основной интерфейс с вкладками).
 */
enum class Screen {
    Splash, Main
}